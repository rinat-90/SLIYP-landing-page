extends ../layout/main

block variables
  - var title = 'SLIYP';
  - var description = 'Honeymoon Landing Page';
  - var pageClass = 'index-page';
  - var imgPath = ''

block content


block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }